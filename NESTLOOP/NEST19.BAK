#include<stdio.h>
#include<conio.h>

void main()
{
	int i,j,l,a=0;
	clrscr();
	printf("Enter limit:\t");
	scanf("%d",&l);
	for(i=l;i<=l;)
	{	for(j=1;j<=l-i;j++)
			printf(" ");
		for(j=1;j<=i;j++)
			printf("* ");
		printf("\n");
		if (a==0)
			i--;
		else
			i++;
		if (i==1)
			a=1;
	}
	getch();
}